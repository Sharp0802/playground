#include "ToolPanel.h"

HRESULT ToolPanel::Render(
    const int width, 
    const int height, 
    Palette* palette, 
    Theme* theme, 
    ID2D1HwndRenderTarget* renderTarget)
{
    float widthF = static_cast<float>(width);
    float heightF = static_cast<float>(height);

    HRESULT hr = S_OK;

    return hr;
}
